pipeline {
    agent any

    stages {
        stage('Trigger AI Jenkins Job') {
            steps {
                script {
                    sh '''
                        curl -X POST http://10.0.2.24:8080/job/chatbot/build?token=my-trigger-token
                    '''
                }
            }
        }
    }
}
